import { Alert, DistressParseResult, Snapshot, Zone } from '../types'

export function saveAlert(_alert: Alert): void {
  // Supabase is intentionally fire-and-forget and enabled after core demo flow.
}

export function saveZone(_zone: Zone): void {
}

export function saveSnapshot(_snapshot: Snapshot): void {
}

export function saveDistress(_distress: DistressParseResult): void {
}
